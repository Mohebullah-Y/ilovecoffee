import { WrapResponsInterceptor } from './wrap-respons.interceptor';

describe('WrapResponsInterceptor', () => {
  it('should be defined', () => {
    expect(new WrapResponsInterceptor()).toBeDefined();
  });
});
